// Modules (exports)
// Named Exports
export const PI = 3.14159;

export const add = (a, b) => a + b;

// Default Export
const subtract = (a, b) => a - b;
export default subtract;

// NOTE
// Named Exports: You can have multiple named exports per file.
// Default Export: You can only have one default export per file. It’s often used for the main functionality.
